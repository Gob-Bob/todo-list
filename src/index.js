import './style.css'

// This file will be used for the logic behind everything else
// Create a separate JS file that is specfically used for creating module functions related to the DOM
    // Create a factory function to create new projects
// Create a separarate JS file that is only used for creating elements
    // Create a container that houses all of the projects
